#include <stdio.h>
#include <conio.h>
int main()
{
    int a,b,i,gcd;
    printf("Enter the two numbers for GCD: \n\n");
    scanf("%d%d",&a,&b);

    for(i=1;i<=a&&b;i++)
    {
        if(a%i==0 && b%i==0)
        gcd=i;
    }
        printf("G.C.D of %d and %d is %d",a,b,gcd);
}
